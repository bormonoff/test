name: Deploy

on:
  push:
    tags:
      - 'v*'

jobs:
  update-tag:
    runs-on: ubuntu-latest
    steps:
      - name: gen token
        uses: actions/create-github-app-token@v3
        id: app-token
        with:
          client-id: ${{ vars.APP_CLIENT_ID }}
          private-key: ${{ secrets.APP_PRIVATE_KEY }}
      - name: set tag base
        run: |
          TAG=${{ github.ref_name }}
          echo "VERSION=${TAG#v}" >> $GITHUB_ENV
      - uses: actions/checkout@v4
        with:
          ref: main
          token: ${{ steps.app-token.outputs.token }}
      - run: |
          git config user.name "ge-b4n-ci[bot]"
          git config user.email "my-bot[bot]@users.noreply.github.com"

          sed -i "s|bormonoff/bridge-api:.*|bormonoff/bridgeapi:${{ env.VERSION }}|g" docker-compose.yml

          git add .
          git commit -m "Auto-generated changes" || exit 0
          git push